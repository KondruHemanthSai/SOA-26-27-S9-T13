server:
  port: 8082

spring:
  application:
    name: student-service
  data:
    mongodb:
      uri: ${MONGODB_STUDENT_URI:${MONGODB_URI:${MONGODB_BASE_URI:mongodb://localhost:27017}/student_central_student${MONGODB_URI_OPTIONS:}}}

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER_URL:http://localhost:8761/eureka/}
    fetch-registry: true
    register-with-eureka: true
  instance:
    prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

jwt:
  secret: ${JWT_SECRET:studentCentralSuperSecretJwtKeyWithSufficientLengthForHS256Security2024!}

logging:
  level:
    com.studentcentral.student: INFO
    org.springframework.data.mongodb: INFO

