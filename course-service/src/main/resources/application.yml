server:
  port: 8084

spring:
  application:
    name: course-service
  data:
    mongodb:
      uri: ${MONGODB_COURSE_URI:${MONGODB_URI:${MONGODB_BASE_URI:mongodb://localhost:27017}/student_central_course${MONGODB_URI_OPTIONS:}}}

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER_URL:http://localhost:8761/eureka/}
    fetch-registry: true
    register-with-eureka: true
  instance:
    prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

jwt:
  secret: ${JWT_SECRET:studentCentralSuperSecretJwtKeyWithSufficientLengthForHS256Security2024!}

logging:
  level:
    com.studentcentral.course: INFO
    org.springframework.data.mongodb: INFO

