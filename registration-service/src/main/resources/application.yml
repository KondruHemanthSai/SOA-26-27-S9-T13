server:
  port: 8085

spring:
  application:
    name: registration-service
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/student_central_registration}

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER_URL:http://localhost:8761/eureka/}
    fetch-registry: true
    register-with-eureka: true
  instance:
    prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

jwt:
  secret: ${JWT_SECRET:studentCentralSuperSecretJwtKeyWithSufficientLengthForHS256Security2024!}

registration:
  max-credits-per-semester: ${MAX_CREDITS_PER_SEMESTER:24}

logging:
  level:
    com.studentcentral.registration: INFO
    org.springframework.data.mongodb: INFO

